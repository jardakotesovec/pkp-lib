@if ($doiObject)
    @php
        $doiUrl = $doiObject->getData('resolvingUrl');
    @endphp
    <x-metadata.default
        slug="doi"
        title="{{ __('doi.readerDisplayName') }}"
        :$headingTag
    >
        <a href="{{ $doiUrl }}" class="link">
            {{ $doiUrl }}
        </a>
    </x-metadata.default>
@endif
