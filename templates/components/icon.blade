{{-- SVG Icon Component --}}
@props(['name', 'class' => 'w-5 h-5'])

@php
    // Register icon so svg-icon-sprite.blade will include it
    \PKP\facades\Frontend::addIcons([$name]);
@endphp

<svg {{ $attributes->merge(['class' => $class]) }} aria-hidden="true">
    <use href="#icon-{{ $name }}" />
</svg>
